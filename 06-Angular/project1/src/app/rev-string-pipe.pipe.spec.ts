import { RevStringPipePipe } from './rev-string-pipe.pipe';

describe('RevStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RevStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
